apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mortgage.fullname" . }}-test-connection"
  labels:
    {{- include "mortgage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mortgage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
